import { HideElementsDirective } from './hide-elements.directive';

describe('HideElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new HideElementsDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
